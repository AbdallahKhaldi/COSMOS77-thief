"""Team identity constants shared verbatim by both repos (raw-copied, never token-swapped).

The ``repos`` mapping keys are wire-identity keys from the greeting schema; they must point at
the same URLs from both halves of the team.
"""

from __future__ import annotations

GROUP_ID = "cosmos77"
GROUP_NAME = "cosmos77"
LLM_MODEL = "gemini-2.5-flash"
MEMBER_IDS = ["323118794", "212389712"]
TEAM_REPOS = {
    "cop": "https://github.com/AbdallahKhaldi/COSMOS77-cop",
    "thief": "https://github.com/AbdallahKhaldi/COSMOS77-thief",
}
